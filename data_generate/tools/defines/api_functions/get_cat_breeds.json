{
  "name": "get_cat_breeds",
  "description": "Get all cat breeds from the Cat API. Returns a list of all available cat breeds with their information.",
  "parameters": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "integer",
        "description": "Number of results to return (1-100)",
        "minimum": 1,
        "maximum": 100
      },
      "page": {
        "type": "integer",
        "description": "Page number for pagination",
        "minimum": 0
      },
      "save_to": {
        "type": "string",
        "description": "File path to save the result as JSON"
      }
    },
    "required": []
  }
}