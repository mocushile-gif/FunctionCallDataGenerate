{
    "name": "get_foreign_keys",
    "description": "Retrieve all foreign key constraints for a specified table in the SQLite database.",
    "parameters": {
        "type": "object",
        "properties": {
            "database_path": {
                "type": "string",
                "description": "The path to the database."
            },
            "table_name": {
                "type": "string",
                "description": "The name of the table to query for foreign key constraints."
            }
        },
        "required": ["database_path", "table_name"]
    }
}
