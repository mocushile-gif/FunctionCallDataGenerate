{
    "name": "get_column_names",
    "description": "Retrieve column names for a specified table in a SQLite database.",
    "parameters": {
        "type": "object",
        "properties": {
            "database_path": {
                "type": "string",
                "description": "The path to the database."
            },
            "table_name": {
                "type": "string",
                "description": "The name of the table to retrieve column names from."
            }
        },
        "required": ["database_path", "table_name"]
    }
}
