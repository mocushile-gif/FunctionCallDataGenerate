{
    "name": "get_indexes",
    "description": "Retrieve all indexes of a specified table in the SQLite database.",
    "parameters": {
        "type": "object",
        "properties": {
            "database_path": {
                "type": "string",
                "description": "The path to the database."
            },
            "table_name": {
                "type": "string",
                "description": "The name of the table to retrieve indexes from."
            }
        },
        "required": ["database_path", "table_name"]
    }
}
