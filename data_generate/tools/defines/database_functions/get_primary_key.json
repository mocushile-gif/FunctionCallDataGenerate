{
    "name": "get_primary_key",
    "description": "Retrieve the primary key column(s) of a specified table in the SQLite database.",
    "parameters": {
        "type": "object",
        "properties": {
            "database_path": {
                "type": "string",
                "description": "The path to the database."
            },
            "table_name": {
                "type": "string",
                "description": "The name of the table to retrieve the primary key column(s) from."
            }
        },
        "required": ["database_path", "table_name"]
    }
}
