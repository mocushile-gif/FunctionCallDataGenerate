{
    "name": "delete_from_table",
    "description": "Perform a DELETE query on a specified SQLite table to remove rows.",
    "parameters": {
        "type": "object",
        "properties": {
            "database_path": {
                "type": "string",
                "description": "The path to the database."
            },
            "table_name": {
                "type": "string",
                "description": "The name of the table to delete rows from."
            },
            "where_clause": {
                "type": "string",
                "description": "The WHERE clause to filter rows for deletion, without the 'WHERE' keyword. Optional.",
                "default": null
            },
            "where_params": {
                "type": "array",
                "items": {
                    "type": "string"
                },
                "description": "A list of parameters to bind to the WHERE clause. Optional.",
                "default": []
            }
        },
        "required": ["database_path", "table_name"]
    }
}
