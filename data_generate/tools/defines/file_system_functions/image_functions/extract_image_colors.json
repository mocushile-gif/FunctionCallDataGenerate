{
  "name": "extract_image_colors",
  "description": "Extract dominant colors from an image and generate a color palette.",
  "parameters": {
    "type": "object",
    "properties": {
      "image_path": {
        "type": "string",
        "description": "Path to the input image file"
      },
      "num_colors": {
        "type": "integer",
        "description": "Number of dominant colors to extract (1-20)",
        "default": 5,
        "minimum": 1,
        "maximum": 20
      },
      "output_path": {
        "type": "string",
        "description": "Path for the output color palette image. If not provided, will create auto-named file."
      }
    },
    "required": ["image_path"]
  }
} 