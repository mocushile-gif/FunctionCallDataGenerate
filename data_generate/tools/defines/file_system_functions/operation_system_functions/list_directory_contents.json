{
    "name": "list_directory_contents",
    "description": "List the contents of a directory with options for hidden files and detailed output.",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "The path of the directory to list. Default to './'"
            },
            "show_hidden": {
                "type": "boolean",
                "description": "Whether to include hidden files. Defaults to False.",
                "default": false
            },
            "long_format": {
                "type": "boolean",
                "description": "Whether to use long format listing (similar to `ls -l`). Defaults to False.",
                "default": false
            }
        },
        "required": ["path"]
    }
}
