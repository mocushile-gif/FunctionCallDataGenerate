{
    "name": "change_permissions",
    "description": "Change the permissions of a file or directory.",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "The path of the file or directory. Default to './'"
            },
            "mode": {
                "type": "integer",
                "description": "The permissions to set (in integer format, e.g., 493)."
            }
        },
        "required": ["path", "mode"]
    }
}
