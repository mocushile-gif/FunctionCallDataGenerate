{
  "name": "compress_to_zip",
  "description": "Compress a file or directory into a ZIP archive.",
  "parameters": {
    "type": "object",
    "properties": {
      "src": {
        "type": "string",
        "description": "The source file or directory to compress. Default to './'"
      },
      "zip_name": {
        "type": "string",
        "description": "The name of the ZIP file to create."
      },
      "include_root": {
        "type": "boolean",
        "description": "Whether to include the root directory in the archive. Defaults to True.",
        "default": true
      }
    },
    "required": ["src", "zip_name"]
  }
}
