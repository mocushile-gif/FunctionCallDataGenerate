{
    "name": "delete_files_by_extension",
    "description": "Delete all files with a specific extension in a directory.",
    "parameters": {
        "type": "object",
        "properties": {
            "directory": {
                "type": "string",
                "description": "The directory to search in. Default to './'"
            },
            "file_extension": {
                "type": "string",
                "description": "The file extension to delete (e.g., '.log')."
            },
            "recursive": {
                "type": "boolean",
                "description": "Whether to search subdirectories. Defaults to True.",
                "default": true
            }
        },
        "required": ["directory", "file_extension"]
    }
}
