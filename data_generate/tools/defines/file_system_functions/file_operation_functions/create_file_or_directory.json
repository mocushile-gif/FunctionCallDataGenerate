{
    "name": "create_file_or_directory",
    "description": "Create a file or directory with specified permissions.",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "The path of the file or directory to create. Default to './'"
            },
            "is_directory": {
                "type": "boolean",
                "description": "Whether to create a directory instead of a file. Defaults to False."
            },
            "mode": {
                "type": "integer",
                "description": "The permissions to set. Defaults to 493."
            }
        },
        "required": ["path"]
    }
}
