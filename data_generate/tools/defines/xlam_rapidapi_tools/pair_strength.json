{
    "name": "pair_strength",
    "description": "This is a sub-function API for tool currency_quake.\nTool description: Provides Access to the statistical strength of all 8 major currencies, across 28 Forex pairs and on 4 different time frames.\nAPI description: Exposes the underlying calculations that are performed on each pair to get a currency’s combined strength.",
    "parameters": {
        "type": "object",
        "properties": {
            "timeframe": {
                "type": "string",
                "description": "The timeframe for which to retrieve the currency pair strength (e.g., 'h1', 'h4', 'd1', 'w1').",
                "default": "h1"
            }
        },
        "required": [
            "timeframe"
        ]
    }
}