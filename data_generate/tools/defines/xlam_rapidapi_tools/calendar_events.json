{
    "name": "calendar_events",
    "description": "This is a sub-function API for tool yh_finance_complete.\nTool description: This API helps you to query stocks, quotes, movers and other financial summary.\nAPI description: Fetches the calendar events for a particular stock symbol using the Yahoo Finance API via RapidAPI.",
    "parameters": {
        "type": "object",
        "properties": {
            "symbol": {
                "type": "string",
                "description": "The stock symbol to fetch calendar events for."
            }
        },
        "required": [
            "symbol"
        ]
    }
}