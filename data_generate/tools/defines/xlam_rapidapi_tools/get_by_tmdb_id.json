{
    "name": "get_by_tmdb_id",
    "description": "This is a sub-function API for tool mdblist.\nTool description: Get Movies and TV Shows data with ratings from multiple providers in JSON format\nAPI description: Fetches movie or TV show information based on a given TMDb ID.",
    "parameters": {
        "type": "object",
        "properties": {
            "tm": {
                "type": "number",
                "description": "A valid TMDb ID for the movie or TV show.",
                "default": 578.0
            },
            "m": {
                "type": "string",
                "description": "Mediatype *movie* or *show* (default *movie*)"
            }
        },
        "required": [
            "tm"
        ]
    }
}