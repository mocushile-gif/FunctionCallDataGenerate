{
    "name": "addresses_addressid",
    "description": "This is a sub-function API for tool addressr.\nTool description: Australian Address Validation, Search and Autocomplete\nAPI description: Fetch detailed information about a specific address using the given address ID.",
    "parameters": {
        "type": "object",
        "properties": {
            "addressid": {
                "type": "string",
                "description": "The ID of the address to fetch details for."
            }
        },
        "required": [
            "addressid"
        ]
    }
}