{
    "name": "specificcolorset",
    "description": "This is a sub-function API for tool colorme.\nTool description: Get some colors either randomly selected or based on your input base color\nAPI description: Fetch a set of colors based on the input color name using the ColorMe API.",
    "parameters": {
        "type": "object",
        "properties": {
            "colorname": {
                "type": "string",
                "description": "The name of the base color to generate a set of colors from (e.g., \"yellow\", \"red\").",
                "default": "yellow"
            }
        },
        "required": [
            "colorname"
        ]
    }
}