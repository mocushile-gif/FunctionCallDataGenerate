{
    "name": "get_random_fact",
    "description": "This is a sub-function API for tool numbers.\nTool description: An API for interesting facts about numbers. Provides trivia, math, date, and year facts about numbers. \r\n\r\nFor example, \"5 is the number of platonic solids\", \"42 is the number of little squares forming the left side trail of Microsoft's Windows 98 logo\", \"February 27th is the day in 1964 that the government of Italy asks for help to keep the Leaning Tower of Pisa from toppling over\"\nAPI description: Fetches a random fact based on the specified type and optional parameters.",
    "parameters": {
        "type": "object",
        "properties": {
            "type": {
                "type": "string",
                "description": "The category of the fact. Must be one of \"trivia\", \"math\", \"date\", or \"year\".",
                "default": "trivia"
            },
            "min": {
                "type": "string",
                "description": "The minimum number, inclusive. Defaults to '10'.",
                "default": "10"
            },
            "max": {
                "type": "string",
                "description": "The maximum number, inclusive. Defaults to '20'.",
                "default": "20"
            },
            "fragment": {
                "type": "string",
                "description": "Add \"?fragment=true\" to return the fact as a sentence fragment that can be easily included as part of a larger sentence. This means that the first word is lowercase and ending punctuation is omitted. For trivia and math, a noun phrase is returned that can be used in a sentence like “We now have more users than [fact as fragment]!”.",
                "default": "True"
            },
            "json": {
                "type": "string",
                "description": "Specify 'True' to return the result as JSON instead of plaintext. Defaults to 'True'.",
                "default": "True"
            }
        },
        "required": [
            "type"
        ]
    }
}