{
    "name": "get_predictions_performance_statistics",
    "description": "This is a sub-function API for tool daily_betting_tips.\nTool description: We provide 90% hit rate daily soccer and basketball betting predictions, Our predictions are generated by AI powered analysis of previous matches and other factors key to the game's result\nAPI description: This Endpoint is used to check the predictions performance for a given date.\nThe date format for a given date should be \"dd.MM.yyyy\", else response from API will be empty.",
    "parameters": {
        "type": "object",
        "properties": {
            "q": {
                "type": "string",
                "description": "The query string to filter the predictions by date. The date format should be \"dd.MM.yyyy\".",
                "default": "23.12.2020"
            },
            "connection": {
                "type": "string",
                "description": "The connection type for the request. Defaults to 'keep-alive'.",
                "default": "keep-alive"
            },
            "content_type": {
                "type": "string",
                "description": "The content type for the request. Defaults to 'application/json'.",
                "default": "application/json"
            }
        },
        "required": [
            "q"
        ]
    }
}