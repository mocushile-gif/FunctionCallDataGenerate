{
    "name": "minus_di",
    "description": "This is a sub-function API for tool twelve_data.\nTool description: Financial data provided for developers, to enter the world markets. Instant access for real-time and historical data of stocks, forex, crypto, ETFs, indices, and more. Read more in [documentation](https://twelvedata.com/docs) and start [here](https://twelvedata.com/apikey).\nAPI description: Minus Directional Indicator(MINUS_DI) is a component of the Average Directional Index(ADX), and it measures the existence of downtrend.",
    "parameters": {
        "type": "object",
        "properties": {
            "symbol": {
                "type": "string",
                "description": "The instrument symbol, which can be any equity, index, ETF, forex, or cryptocurrency (e.g., 'AAPL', 'EUR/USD', 'ETH/BTC').",
                "default": "AAPL"
            },
            "interval": {
                "type": "string",
                "description": "The interval between two consecutive points in the time series data. Supported intervals are: '1min', '5min', '15min', '30min', '45min', '1h', '2h', '4h', '1day', '1week', '1month'.",
                "default": "1min"
            },
            "outputsize": {
                "type": "number",
                "description": "The number of data points to return. Defaults to 30 if no date parameters are set, otherwise set to maximum.",
                "default": 30
            },
            "time_period": {
                "type": "number",
                "description": "The number of periods to consider in the calculation. Defaults to 9.",
                "default": 9
            },
            "format": {
                "type": "string",
                "description": "The format of the data returned. Can be 'CSV' or 'JSON'. Defaults to 'json'.",
                "default": "json"
            }
        },
        "required": [
            "symbol",
            "interval"
        ]
    }
}