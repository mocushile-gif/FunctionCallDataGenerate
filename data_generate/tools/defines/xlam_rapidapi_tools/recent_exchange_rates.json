{
    "name": "recent_exchange_rates",
    "description": "This is a sub-function API for tool currency_conversion_and_exchange_rates.\nTool description: Simple reliable API for current and historical foreign exchange (forex) currency rates.\nAPI description: Retrieve the latest exchange rate data. Refresh rate will depend on your subscription: updated every 60 minutes, every 10 minutes or every 60 seconds.",
    "parameters": {
        "type": "object",
        "properties": {
            "is_from": {
                "type": "string",
                "description": "Three-letter currency code of the currency would would like to convert from. This currency will be converted into the code of the currency provided in the `to` parameter",
                "default": "USD"
            },
            "to": {
                "type": "string",
                "description": "Comma-separated list of currency codes to convert 'is_from' currency into. Defaults to 'EUR,GBP'.",
                "default": "EUR,GBP"
            }
        },
        "required": []
    }
}