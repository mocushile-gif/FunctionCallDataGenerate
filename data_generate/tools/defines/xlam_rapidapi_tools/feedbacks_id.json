{
    "name": "feedbacks_id",
    "description": "This is a sub-function API for tool aliexpress_unofficial.\nTool description: DEPRECATED. Will deleted after 2023-03-01\nAPI description: Fetches feedbacks for a given product ID from the AliExpress unofficial API.",
    "parameters": {
        "type": "object",
        "properties": {
            "is_id": {
                "type": "number",
                "description": "The product ID for which feedbacks are to be fetched.",
                "default": 4000886597329.0
            },
            "withpictures": {
                "type": "number",
                "description": "Indicates whether to include feedbacks with pictures (1 by default).",
                "default": 1
            },
            "page": {
                "type": "number",
                "description": "Specifies the page number of feedback results to fetch (1 by default).",
                "default": 1
            }
        },
        "required": [
            "is_id"
        ]
    }
}