{
    "name": "generate_random_password",
    "description": "Generates a random password of specified length, character types, and complexity.",
    "parameters": {
        "type": "object",
        "properties": {
            "length": {
                "type": "integer",
                "description": "The length of the password. Defaults to 12.",
                "default": 12,
                "minimum": 1
            },
            "include_special": {
                "type": "boolean",
                "description": "Whether to include special characters in the password. Defaults to True.",
                "default": true
            },
            "complexity": {
                "type": "string",
                "description": "The complexity level of the password. Options: \"low\", \"medium\", \"high\". Defaults to \"medium\".",
                "default": "medium",
                "enum": [
                    "low",
                    "medium",
                    "high"
                ]
            }
        },
        "required": []
    }
}