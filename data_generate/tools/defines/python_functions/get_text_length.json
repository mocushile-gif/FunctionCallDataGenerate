{
    "name": "get_text_length",
    "description": "Calculates the length of a text with options to include/exclude punctuation and numbers. Treats English words as one unit and each Chinese character as one unit.",
    "parameters": {
        "type": "object",
        "properties": {
            "text": {
                "type": "string",
                "description": "The input text."
            },
            "include_punctuation": {
                "type": "boolean",
                "description": "Whether to include punctuation in the count. Defaults to true.",
                "default": true
            },
            "include_numbers": {
                "type": "boolean",
                "description": "Whether to include numbers in the count. Defaults to true.",
                "default": true
            }
        },
        "required": [
            "text"
        ]
    }
}